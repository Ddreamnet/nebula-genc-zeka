node_modules/
.next/
dist/
build/
.env
.env.local
.env*.local
*.log
.vercel

# root package.json has zero deps (it just delegates into web/) — its
# lockfile carries no real info and gets regenerated by every host's own
# automatic install step anyway
/package-lock.json
